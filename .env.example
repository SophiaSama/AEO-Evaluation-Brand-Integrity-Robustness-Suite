# BIRS - Brand Integrity Robustness Suite
# Copy to .env and set values if needed.

# Optional: DeepEval may use OpenAI for judge model (leave empty for local-only)
# DEEPEVAL_OPENAI_API_KEY=

# Optional: Override paths (defaults: data/documents, data/chroma_birs)
# BIRS_DATA_DIR=data
# BIRS_CHROMA_DIR=data/chroma_birs

# Optional: Ollama base URL (default: http://localhost:11434)
# OLLAMA_BASE_URL=http://localhost:11434

# Optional: Ollama model name (default: llama3.2)
# OLLAMA_MODEL=llama3.2
