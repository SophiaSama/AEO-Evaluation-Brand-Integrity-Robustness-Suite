# BIRS - Brand Integrity Robustness Suite (sandboxed)
# Python 3.10+

# Vector store & embeddings
chromadb>=0.4.22
sentence-transformers>=2.2.2

# RAG orchestration
langchain>=0.3.0
langchain-community>=0.3.0

# LLM (local via Ollama - install Ollama separately and pull llama3.2)
# LangChain talks to Ollama over HTTP; no extra pip package required

# Evaluation
deepeval>=0.21.0

# Sentiment (local, no API)
vaderSentiment>=3.3.2

# Web crawling (real-life brand data)
requests>=2.31.0
trafilatura>=1.6.0
duckduckgo-search>=4.0.0

# AEO Audit Guidelines - Entity validation & Schema checking
beautifulsoup4>=4.12.0
extruct>=0.16.0
jsonschema>=4.20.0

# Utilities
python-dotenv>=1.0.0

# Testing & CI/CD
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.5.0
pytest-split>=0.8.0
